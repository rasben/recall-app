# Recall — Agent instructions

This file gives AI coding agents the context needed to work on this project. Keep it accurate: when you change setup, scripts, structure, or conventions, update this file.

---

## What is Recall?

Recall is a desktop app that helps you fill in your Harvest time tracking by building a timeline of what you actually did on a given day. It pulls activity data from multiple sources — GitHub, Google Workspace, local git repos, JIRA, Zulip — and presents them on a single timeline so you can quickly see what you worked on and when.

The target user is a developer at Reload (the company) who uses Harvest for time tracking.

---

## Data sources

Each source provides timestamped activity events that are merged into a day timeline.

| Source | What it captures | Auth / access |
|--------|-----------------|---------------|
| **GitHub** | PRs opened/reviewed/merged, commits pushed, issues commented on | Personal access token or GitHub CLI (`gh`) |
| **Google Calendar** | Meetings and events you attended | Google OAuth (Calendar API) |
| **Gmail** | Emails you **sent** or **replied to** (not all received mail) | Google OAuth (Gmail API) |
| **Google Drive** | Docs/Sheets/Slides you **edited** | Google OAuth (Drive Activity API) |
| **Local git** | Commits across all local repos, by your git author name | Shell: `git log --all --since=... --author=...` |
| **JIRA** | Tickets you commented on, transitioned, or were assigned to | JIRA API token |
| **Zulip** | Messages you **sent** | Zulip API key |

Potential future sources: Browser history, terminal/shell history, Harvest (to show already-logged entries alongside the timeline).

---

## Project structure

| Path | Purpose |
|------|--------|
| `src/` | Frontend: SvelteKit routes, components, app shell |
| `src-tauri/` | Tauri/Rust backend: commands, DB, app state |
| `static/` | Static assets |
| `build/` | Vite build output (Tauri uses this as `frontendDist`; git-ignored) |

**Frontend (`src/`):**

- `src/app.html` — HTML shell with dark mode detection
- `src/bindings.ts` — **Generated by tauri-specta from Rust commands. Do not edit.** Re-generated in debug builds via `lib.rs`.
- `src/components/Main.svelte` — Main tab content (will become the timeline view)
- `src/components/Settings.svelte` — Settings tab (theme switcher + source credentials)
- `src/lib/components/ui/` — shadcn-svelte UI primitives (button, card, tabs, select, toggle, sonner, etc.)
- `src/lib/utils.ts` — `cn()` helper and utility types
- `src/routes/` — SvelteKit routes: `+layout.svelte`, `+page.svelte` (tab shell), `layout.css` (retro theme)

**Backend (`src-tauri/`):**

- `src-tauri/src/lib.rs` — Tauri entry, command registration, tauri-specta export
- `src-tauri/src/commands/settings.rs` — Language get/set + generic key-value helpers (`save_val`, `get_val`)
- `src-tauri/src/commands/harvest_done.rs` — Load/save timeline Harvest checkmarks (SQLite rows keyed by UUID v5 of `TimelineEvent.id`)
- `src-tauri/src/db.rs` — SQLite init (`settings` key-value table; `timeline_harvest_done` for per-event Harvest checkmarks)
- `src-tauri/src/state.rs` — AppState with DB mutex
- `src-tauri/capabilities/` — Tauri permissions
- `src-tauri/icons/` — App icons

---

## Tech stack

| Layer | Technology |
|-------|------------|
| Frontend | Svelte 5, SvelteKit 2, Vite 6, TypeScript (strict, ~5.6.2) |
| Styling | Tailwind CSS 4 (`@tailwindcss/vite`), tw-animate-css, @tailwindcss/forms, @tailwindcss/typography |
| UI | bits-ui (shadcn-svelte), svelte-sonner, @lucide/svelte |
| Desktop | Tauri 2 |
| Backend | Rust (edition 2021), tauri-specta, rusqlite, once_cell, uuid (v5 for Harvest-done row ids) |
| Package manager | npm (lockfile: `package-lock.json`) |

---

## Key config files

| File | Role |
|------|------|
| `package.json` | npm scripts, deps; `"tauri": "tauri"` |
| `vite.config.js` | Vite + Tailwind + SvelteKit; dev port **1420**, Tauri dev host |
| `svelte.config.js` | SvelteKit: `adapter-static`, alias `@/*` → `./lib/*` |
| `tsconfig.json` | Extends `.svelte-kit/tsconfig.json`, strict, bundler moduleResolution |
| `src-tauri/Cargo.toml` | Rust deps |
| `src-tauri/tauri.conf.json` | Tauri 2: beforeDevCommand/beforeBuildCommand, frontendDist `../build` |
| `components.json` | shadcn-svelte (Tailwind, `$lib` aliases) |

---

## Workflows

### Install

- From repo root: `npm install`. Rust: `cargo` in `src-tauri/` (no extra install step).

### Dev

- Frontend only: `npm run dev` → Vite at **http://localhost:1420**
- Full app: `npm run tauri dev` (runs `npm run dev` then opens Tauri window).

### Build

- Frontend: `npm run build` → output in `build/`
- Desktop: `npm run tauri build` (runs `npm run build` then Tauri bundle).

### Typecheck & format

- `npm run check` — SvelteKit sync + svelte-check
- `npm run format` — Prettier on `src/` only

---

## Conventions

- **Do not edit `src/bindings.ts`** — It is generated by tauri-specta in debug builds. Add or change Tauri commands in `src-tauri/src/commands/` and register them in `lib.rs`.
- **Imports** — Use `$lib` for lib (SvelteKit default); `@/*` → `./lib/*`. Frontend imports commands and types from `../bindings`.
- **Svelte** — PascalCase for main components; `src/lib/components/ui/` uses kebab-case (e.g. `loader.svelte`).
- **Rust** — snake_case for modules and command names; commands exposed via tauri-specta in `lib.rs`.
- **Settings storage** — Generic key-value store in SQLite (`settings` table). Use `save_val`/`get_val` in `commands/settings.rs` for new settings. Source credentials (tokens, API keys) are stored here.
- **Timeline events** — Each data source should produce a common event shape (timestamp, source name, summary text, optional URL) so the frontend can render a unified timeline.

---

## Keeping this file up to date

When you add or change any of the following, update this file so it stays accurate for future agents:

- Scripts in `package.json` or build/config (Vite, SvelteKit, Tauri)
- Project layout (new top-level dirs, moved frontend/backend code)
- Conventions (format, lint, test, naming)
- Env vars or secrets handling
- Dependencies or tech stack (e.g. new UI lib, new Rust crate)
- Data sources (new source, changed auth method, changed API)
