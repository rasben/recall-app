{
  "name": "recall-telemetry-worker",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^4.1.5",
    "wrangler": "^4.103.0"
  }
}
