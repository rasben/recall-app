# Claude — read AGENTS.md

The canonical project context for AI agents lives in [`AGENTS.md`](./AGENTS.md). Read it before making any changes.

It covers: what Recall is, the data sources, project layout (`src/`, `src-tauri/`), settings/timeline conventions, tech stack, key config files, dev/build/check commands, and the "do not edit" files (notably `src/bindings.ts`, which is regenerated by tauri-specta).

When you change setup, scripts, structure, conventions, or data sources, update `AGENTS.md` — do not duplicate that information here.
